include "target.tmh"

//program startup
int main()
{
    if(Init(&EventHandle)) return 1; // declare the event handler, return on error
	
	//add initialization code here
	Configure(&LMFD,MODE_EXCLUDED);
	Configure(&RMFD,MODE_EXCLUDED);	
	Configure(&Joystick,MODE_EXCLUDED);
	Configure(&TFRPRudder,MODE_EXCLUDED);
	Configure(&TFRPHARudder,MODE_EXCLUDED);	
	
MapKey	(&Throttle,	APENG,	EXEC("launch_mfdisplay();"));
}

//event handler
int EventHandle(int type, alias o, int x)
{
    DefaultMapping(&o, x);
	
	//add event handling code here
}
int launch_mfdisplay()
{
GameOutput(&Throttle, OUT_ID_LED_1, 2);
printf("attempting to launch MFD4CTS\xa");
int launched = system("spawn -w \"C:\\Program Files (x86)\\Vyper Industries\\MFD 4 CTS\" \"C:\\Program Files (x86)\\Vyper Industries\\MFD 4 CTS\\MFD4CTS.exe\" mod:FA-18C"); 
printf("Result was %d", launched);
}